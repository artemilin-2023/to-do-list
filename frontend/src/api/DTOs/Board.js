export class Board {
  constructor(id, title, description, createdAt, tags) {
    this.id = id;
    this.title = title;
    this.description = description;
    this.createdAt = createdAt;
    this.tags = tags;
  }
}
