export class Board {
  constructor(id, name, description, createdAt, tags) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.createdAt = createdAt;
    this.tags = tags;
  }
}
