export default class User {
  constructor(id, nickname, email, bio, avatar) {
    this.id = id;
    this.nickname = nickname;
    this.email = email;
    this.bio = bio;
    this.avatar = avatar;
  }
}
